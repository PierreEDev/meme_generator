import { render } from '@testing-library/react';
import { ImageForm } from '../../components/ImageForm/ImageForm';

describe('ImageForm Component Test', () => {
  it('should have form input', () => {

  });

  it('should return the created meme link', () => {

  });
});
