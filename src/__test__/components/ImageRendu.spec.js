import { fireEvent, render } from '@testing-library/react';
import { ImageRendu } from '../../components/ImageRendu/ImageRendu';

describe('ImageRendu Component Test', () => {
  it('should render a meme or <></>', () => {

  });
});
